/**
 * config/tokens.js
 *
 * Legacy token config — no longer used.
 * Tokens are now managed via:
 *   1. MASTER_API_TOKEN env var (all-domain access)
 *   2. Database-stored scoped tokens (managed in admin UI at /tokens)
 */

module.exports.tokens = {};
